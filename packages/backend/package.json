{
  "name": "oauth-relay-example-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "firebase emulators:start --only functions",
    "dev:standalone": "tsx src/standalone.ts",
    "deploy": "firebase deploy --only functions",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^5.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": "22"
  }
}
